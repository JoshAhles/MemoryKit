{
  "name": "memorykit-landing",
  "version": "0.1.0",
  "private": true,
  "description": "Static landing page for MemoryKit, a persistent, user-owned memory layer for AI.",
  "scripts": {
    "dev": "npx live-server . --port=4173 --open=/index.html"
  }
}

